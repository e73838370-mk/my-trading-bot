// config.js
// Leave these strings empty until your API key is reset.
export const API_CONFIG = {
    apiKey: "",      // INJECT KEY HERE AFTER RESET
    apiSecret: "",   // INJECT SECRET HERE AFTER RESET
    exchangeId: 'binance' 
};